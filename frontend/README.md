# React + TypeScript + Vite

Node 23+ is required.

Install dependencies with force (a few dependencies are not configured for React Canary):

```bash
npm install --force
```

Run the development server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```